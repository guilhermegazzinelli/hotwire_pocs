<%= form_for(@user) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.fields_for :user_address do |a| %>
    <%= render 'users/address/fields', address: a %>
  <% end %>

  <%= f.submit %>
<% end %>
