<%= form_for(user) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <user-address id="address">
    <%= render 'users/address/fields', user_address: user.user_address %>
  </user-address>

  <%= f.submit %>
<% end %>
